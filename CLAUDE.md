## Environment

The `.env` file is located at the project root and contains a few variables.

One that you will find useful is:

- `DATABASE_URL` — primary database connection string

When database access is needed (for local deployment only), load from `.env` at the root.

## Agent auto tagging on commits author

Please don't add yourself as author when creating commits or PR
